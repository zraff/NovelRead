// Copy this file to supabase-config.js after creating a free Supabase project.
// The app intentionally does not commit real credentials.
const config = {
  url: 'https://YOUR_PROJECT.supabase.co',
  anonKey: 'YOUR_PUBLIC_ANON_KEY'
};
window.SUPABASE_CONFIG = config;
export default config;
